// Generated by CoffeeScript 1.10.0
(function() {
  var Generator, genType, root, typeToSize;

  root = typeof exports !== "undefined" && exports !== null ? exports : this;

  typeToSize = {
    char: 1,
    short: 2,
    int: 4,
    long: 8
  };

  genType = function() {
    var index;
    index = Math.floor(Math.random() * 4);
    return ['char', 'short', 'int', 'long'][index];
  };

  root.Generator = Generator = (function() {
    function Generator() {}

    Generator.basic = function() {
      var arraySize, b, code, genB, genE, genS, genSize, i, index, type;
      genB = function() {
        return 1 << Math.floor(Math.random() * 2 + 2);
      };
      genS = function() {
        return 1 << Math.floor(Math.random() * 2 + 2);
      };
      genE = function() {
        return 1;
      };
      genSize = function() {
        return 2 * (Math.floor(Math.random() * 9 + 2));
      };
      i = genSize();
      index = Math.floor(Math.random() * 4);
      type = genType();
      arraySize = i * typeToSize[type];
      b = genB();
      while (arraySize % b !== 0) {
        i = genSize();
        b = genB();
        type = genType();
        arraySize = i * typeToSize[type];
      }
      code = type + " array[" + i + "];\n\nfor (int i = 0; i < " + i + "; ++i) {\n  array[i] = 10;\n}";
      return {
        code: code,
        s: genS(),
        b: b,
        E: genE()
      };
    };

    Generator.easy = function() {
      var arraySize, b, code, genB, genE, genS, genSize, i, index, j, loops, type;
      genB = function() {
        return 1 << Math.floor(Math.random() * 3 + 3);
      };
      genS = function() {
        return 1 << Math.floor(Math.random() * 3 + 2);
      };
      genE = function() {
        return Math.floor(Math.random() * 2 + 1);
      };
      genSize = function() {
        return 2 * (Math.floor(Math.random() * 9 + 2));
      };
      i = genSize();
      j = genSize();
      type = genType();
      arraySize = typeToSize[type] * i * j;
      b = genB();
      while (arraySize % b !== 0) {
        i = genSize();
        j = genSize();
        b = genB();
        type = genType;
        arraySize = typeToSize[type] * i * j;
      }
      loops = ["for (int i = 0; i < " + i + "; ++i)", "for (int j = 0; j < " + j + "; ++j)"];
      index = 1;
      code = type + " array[" + i + "][" + j + "];\n\n" + loops[Math.abs(index - 1)] + " {\n  " + loops[index] + " {\n    array[i][j] = 15;\n  }\n}";
      return {
        code: code,
        s: genS(),
        b: b,
        E: genE()
      };
    };

    Generator.medium = function() {
      var arraySize, b, code, genB, genE, genS, genSize, i, index, j, loops, type;
      genB = function() {
        return 1 << Math.floor(Math.random() * 3 + 3);
      };
      genS = function() {
        return 1 << Math.floor(Math.random() * 3 + 2);
      };
      genE = function() {
        return Math.floor(Math.random() * 2 + 1);
      };
      genSize = function() {
        return 2 * (Math.floor(Math.random() * 9 + 7));
      };
      i = genSize();
      j = genSize();
      type = genType();
      arraySize = typeToSize[type] * i * j;
      b = genB();
      while (arraySize % b !== 0) {
        i = genSize();
        j = genSize();
        b = genB();
        type = genType;
        arraySize = typeToSize[type] * i * j;
      }
      loops = ["for (int i = 0; i < " + i + "; ++i)", "for (int j = 0; j < " + j + "; ++j)"];
      index = 0;
      code = type + " array[" + i + "][" + j + "];\n\n" + loops[Math.abs(index - 1)] + " {\n  " + loops[index] + " {\n    array[i][j] = 15;\n  }\n}";
      return {
        code: code,
        s: genS(),
        b: b,
        E: genE()
      };
    };

    return Generator;

  })();

}).call(this);
